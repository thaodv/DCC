<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <import type="io.wexchain.android.dcc.vm.ViewModelHelper"/>

        <variable
            name="getRecordBean"
            type="io.wexchain.dccchainservice.domain.redpacket.GetRecordBean"/>

    </data>

    <RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:paddingLeft="10dp"
        android:paddingRight="10dp">

        <TextView
            android:id="@+id/tv_nick_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_marginLeft="16dp"
            android:textColor="@color/FF4A4A4A"
            android:textSize="14sp"
            tools:text="微信昵称"/>

        <TextView
            android:id="@+id/tv_amount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_centerVertical="true"
            android:text="@{ViewModelHelper.showRedPacketGetMoney(getRecordBean.amount)}"
            android:textColor="@color/FF4A4A4A"
            android:textSize="14sp"
            tools:text="￥5.00 红包"/>


        <TextView
            android:id="@+id/tv_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"
            android:text="@{ViewModelHelper.showRedPacketGetTime(getRecordBean.receiveTime)}"
            android:textColor="@color/FF4A4A4A"
            android:textSize="14sp"
            tools:text="08-08 08:08:08"/>

    </RelativeLayout>

</layout>
